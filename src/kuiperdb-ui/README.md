# KuiperDB Explorer

A small React UI for the vector-first HTTP API. It can create named vector spaces, upsert records with caller-supplied vectors, list records, and run vector queries.

```bash
npm ci
npm run dev
```

Set `VITE_KUIPERDB_URL` when the server is not at `http://localhost:17001`.
